*.o
*.a
.cache/
# Do not ignore .git directory, otherwise the reported build number will always be 0
.github/
.gitignore
.vs/
.vscode/
.DS_Store

build*/

tools/ui/node_modules/
tools/server/tests/

scripts/qwen38/
docker-compose.yml
run*.sh

# churns on every bench run; keep the build context hash stable so the
# COPY layer (and the cmake build after it) stays cached
docker/results/
docker/.venv-speed-bench/
docker/.hf-cache/

models/*

/llama-cli
/llama-quantize

arm_neon.h
compile_commands.json
Dockerfile
